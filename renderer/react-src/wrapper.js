const React = require('react');

const Wrapper = () => {
  // eslint-disable-next-line no-console
  console.log('hello');

  return (
    <div>
      <h1>Hello</h1>
    </div>
  );
};

module.exports = Wrapper;
